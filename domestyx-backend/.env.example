# Core
DEBUG=False
ENVIRONMENT=production
SECRET_KEY=change-this-to-a-long-random-secret
ALLOWED_HOSTS=domestix.onrender.com,localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=https://domestix.onrender.com,http://localhost:5173

# Database (use DATABASE_URL in production if available)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
DB_ENGINE=django.db.backends.mysql
DB_NAME=domestyx_db
DB_USER=root
DB_PASSWORD=change-this
DB_HOST=127.0.0.1
DB_PORT=3306

# Email provider (production: use API-based provider integration)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_TIMEOUT=10
RESEND_API_KEY=change-this
DEFAULT_FROM_EMAIL=Domestyx <no-reply@yourdomain.com>

# OTP policy
OTP_EXPIRY_SECONDS=300
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SECONDS=30
OTP_MAX_SENDS_PER_HOUR=5
OTP_VERIFICATION_WINDOW_SECONDS=1800
OTP_REQUIRE_VERIFIED_EMAIL_ON_REGISTER=True
OTP_REQUIRE_VERIFIED_PHONE_ON_REGISTER=True

# SMS provider for phone OTP
# SMS_PROVIDER=console|twilio
SMS_PROVIDER=console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
